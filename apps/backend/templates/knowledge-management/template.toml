template_id = "knowledge-management"
name = "知识管理"
description = "基于 LLM Wiki 模式的知识管理工作区。支持原始资料摄入、知识合成、知识库检索和知识图谱可视化，让知识可以积累而非重复推导。"
icon = "library"
category = "知识"
default_title = "知识管理"
default_description = "构建和管理可积累的结构化知识库。"
initial_conversation_title = "开始知识管理"
env_kind = "none"
recommended_skills = ["aiasys-platform-skill", "aiasys-canvas-skill", "aiasys-memory-organizer-skill", "pymupdf4llm-pdf-to-markdown-skill", "paddleocr-skill", "aiasys-tool-usage-skill"]

[[files]]
relative_path = "README.md"
content = """# 知识管理工作区

基于 Andrej Karpathy 提出的 LLM Wiki 模式构建，将原始资料转化为可积累、可检索、可关联的结构化知识。

## 核心理念

传统 RAG 每次查询都重新从原始资料中推导答案，没有知识积累。LLM Wiki 模式的核心是**编译知识**：LLM 在资料摄入时就读取、提取、合成，将结果写入结构化的 Markdown wiki。后续查询直接查 wiki，而非反复重读原始资料。

> 原始资料 = 源代码（只读）
> Wiki 页面 = 编译产物（LLM 维护）
> 你的提问 = 对编译产物的查询

## 三层架构

### 1. Raw Sources（原始资料层）
- 路径：`sources/`
- 内容：论文 PDF、网页存档、会议笔记、访谈记录、书籍摘录等
- 规则：**只读**。原始资料是真相来源，不直接修改

### 2. Wiki Layer（知识合成层）
- 路径：`wiki/`
- 内容：概念页面、人物页面、项目页面、术语表
- 格式：Markdown + `[[wikilinks]]` 交叉引用 + YAML frontmatter
- 规则：**由 LLM 维护**。人类审核和提问，LLM 负责写入和更新

### 3. Query & Schema（查询与治理层）
- 路径：`docs/` 和 AIASys 内置能力
- 内容：使用指南、检索配置、图谱定义
- 规则：**由人定义范围**，由 LLM 和系统执行

## 与 AIASys 能力的结合

| 能力 | 作用 | 对应目录/工具 |
|------|------|---------------|
| 知识库（KB） | 文档语义检索、全文检索 | 上传 sources/ 资料后使用知识库查询 |
| 知识图谱（KG） | 实体抽取、关系发现、社区可视化 | 从 wiki 或资料中构建图谱 |
| Agent 工具 | 自动化摄入、合成、更新 | 使用 `UploadDocumentsToKnowledgeBase`、`SearchKnowledgeGraphEntities` 等 |

## 目录说明

```
.
├── sources/          # 原始资料（论文、网页、笔记等）
├── wiki/             # LLM 维护的知识 wiki
│   ├── index.md      # 知识目录与标签索引
│   └── log.md        # 资料摄入日志
├── docs/             # 使用指南
│   ├── knowledge-base-guide.md
│   └── graph-guide.md
├── README.md         # 本文件
└── AGENTS.md         # Agent 工作流规范
```

## 快速开始

1. **放入资料**：将 PDF、网页、笔记等放入 `sources/`
2. **知识摄入**：让 Agent 读取资料，提取关键概念，在 `wiki/` 中创建或更新概念页面
3. **建立索引**：将资料上传到 AIASys 知识库，构建向量索引
4. **构建图谱**：从 wiki 或资料中抽取实体和关系，建立知识图谱
5. **查询与关联**：通过知识库检索相似内容，或通过图谱查看概念关联

## 工作流示意

```
sources/资料  →  [Agent 摄入]  →  wiki/概念页面（更新 + 新建）
                                    ↓
                             [上传至知识库]  →  语义检索
                                    ↓
                             [构建知识图谱]  →  实体关系可视化
```
"""

[[files]]
relative_path = "AGENTS.md"
content = """# 知识管理 Agent 工作流

## 角色与边界

- **人类**：定义知识范围、提出问题、审核 wiki 质量、确认关键决策
- **LLM Agent**：执行资料摄入、概念提取、页面合成、交叉引用建立、索引更新
- **系统**：知识库存储向量索引、图谱存储实体关系、文件系统存储 wiki Markdown

## 工作流阶段

### 阶段 1：资料摄入（Ingest）

**输入**：新资料（PDF、网页、文本、图片等）
**操作**：
1. 将资料放入 `sources/`，保持原始文件名和格式
2. Agent 读取资料全文
3. 在 `wiki/log.md` 中记录摄入时间、来源名称、资料类型

**产出**：`sources/{filename}` + `wiki/log.md` 更新

### 阶段 2：知识路由与合成（Route & Synthesize）

**输入**：已摄入的资料 + 现有 wiki 页面
**操作**：
1. Agent 阅读 `wiki/index.md`，了解当前知识范围
2. 判断资料涉及哪些已有概念，可能触发哪些新概念
3. 对相关概念页面进行合成：保留已有内容，整合新信息
4. 对全新概念创建新页面
5. 在页面底部添加 `## 来源` 段落，记录该资料的贡献

**关键规则**：
- 合成时必须保留已有知识，不能丢弃旧内容
- 新信息与旧信息矛盾时，在页面中标注争议点，不直接覆盖
- 每个概念页面保持单一主题，不要写成资料摘要

**产出**：更新或新建的 `wiki/{concept}.md` 页面

### 阶段 3：索引构建（Index）

**输入**：原始资料 + wiki 页面
**操作**：
1. 将原始资料上传到 AIASys 知识库（使用 `UploadDocumentsToKnowledgeBase`）
2. 选择合适的检索模式：
   - 概念探索 → `vector`（语义检索）
   - 精确查找 → `fulltext`（全文检索）
   - 综合查询 → `hybrid`（混合检索）
3. 记录知识库 ID 和配置到 `docs/knowledge-base-guide.md`

**产出**：可检索的知识库 + 配置文档更新

### 阶段 4：图谱构建（Graph）

**输入**：wiki 概念页面或原始资料
**操作**：
1. 使用 `UploadDocumentsToGraph` 将资料上传到知识图谱
2. 系统自动抽取实体和关系
3. Agent 审核抽取结果，手工补充遗漏的重要关系
4. 使用 `GetCommunityReport` 生成社区报告，发现知识聚类
5. 记录图谱 ID 和关键实体到 `docs/graph-guide.md`

**产出**：知识图谱 + 社区报告 + 可视化数据

### 阶段 5：查询与关联（Query）

**输入**：用户问题
**操作**：
1. 优先查 wiki：Agent 读取相关概念页面直接回答
2. wiki 不足时，查知识库：使用 `KnowledgeBaseQuery` 做语义检索
3. 需要关联分析时，查图谱：使用 `SearchKnowledgeGraphEntities` 和 `QueryEntityRelations`
4. 回答中引用来源，标注 `[[页面名]]` 或知识库文档名

**产出**：带出处标注的完整回答

## 页面规范

每个 wiki 页面必须包含：

```markdown
---
title: 页面标题
tags: [标签1, 标签2]
updated: YYYY-MM-DD
sources: [来源文件名1, 来源文件名2]
---

# 页面标题

## 定义
（简洁定义，1-3 句话）

## 核心内容
（详细阐述，可分段）

## 相关概念
- [[概念A]]
- [[概念B]]

## 来源
- `sources/论文.pdf` — 第 3 章
- `sources/网页存档.md` — 第 2 节
```

## 硬性规则

- `sources/` 中的原始资料**只读**，不直接修改
- `wiki/` 中的页面**由 LLM 维护**，人工不直接编辑正文（可评论或提出修改要求）
- 每次摄入后必须更新 `wiki/index.md` 和 `wiki/log.md`
- 概念页面必须使用 `[[slug]]` 建立交叉引用
- 矛盾信息不覆盖，在页面中标注争议
- 优先使用 wiki 回答，不足时才查原始资料
- 知识库和图谱配置变更必须记录到 `docs/` 下的对应指南
"""

[[files]]
relative_path = "sources/.gitkeep"
content = ""

[[files]]
relative_path = "wiki/index.md"
content = """---
title: 知识目录
updated: 2026-01-01
---

# 知识目录

本文件记录当前知识库中所有概念页面及其状态。

## 概念列表

| 页面 | 标签 | 最后更新 | 来源数量 |
|------|------|----------|----------|
| （待 Agent 摄入后填充） | | | |

## 标签索引

（待填充。示例标签：算法、架构、产品、人物、论文、工具）

## 最近更新

（按时间倒序列出最近更新的页面）

## 待建页面

（Agent 在摄入时发现的新概念，但尚未创建页面）
"""

[[files]]
relative_path = "wiki/log.md"
content = """---
title: 摄入日志
updated: 2026-01-01
---

# 摄入日志

按时间顺序记录所有资料摄入操作和 wiki 变更。

## 日志格式

```
[YYYY-MM-DD HH:MM] 摄入: {来源名称}
- 类型: {pdf|web|note|other}
- 更新页面: [[page1]], [[page2]]
- 新建页面: [[page3]]
- 来源路径: sources/{filename}
- 摘要: {一句话概括资料核心内容}
```

## 日志条目

（待 Agent 执行摄入后填充）
"""

[[files]]
relative_path = "docs/knowledge-base-guide.md"
content = """# AIASys 知识库使用指南

知识库是 AIASys 的向量文档检索系统，支持语义检索、全文检索和混合检索。

## 创建知识库

使用 Agent 工具 `CreateKnowledgeBase` 或前端知识库面板创建。

创建时需指定：
- `name`：知识库名称
- `kind`：document（文档型）| structured（结构化）| web（网页型）| code（代码型）
- `embedding_model`：嵌入模型名称（默认使用系统配置的文本模型）
- `chunk_size` / `chunk_overlap`：分块大小和重叠量

## 上传文档

使用 `UploadDocumentsToKnowledgeBase` 或 `UploadDocumentsToKnowledgeBaseBatch` 上传。

支持的格式：PDF、TXT、Markdown、DOCX、HTML 等。

## 检索模式

| 模式 | 适用场景 | 说明 |
|------|----------|------|
| vector | 概念探索、语义相似查询 | 基于向量相似度，召回语义相近但关键词不同的内容 |
| fulltext | 精确关键词查找 | 基于 BM25 或 jieba 分词，适合专有名词、代码片段 |
| hybrid | 综合查询（推荐） | 结合语义和关键词，兼顾召回率和精确度 |

## 查询参数

- `query`：查询文本
- `mode`：检索模式（vector/fulltext/hybrid）
- `top_k`：返回结果数量
- `filters`：按文档名、标签等过滤

## 维护操作

- `ListKnowledgeBaseDocuments`：列出库中文档
- `DeleteDocumentsFromKnowledgeBase`：删除指定文档
- `DeleteKnowledgeBase`：删除整个知识库

## 当前工作区知识库配置

（Agent 在创建知识库后填写）

- 知识库名称：
- 知识库 ID：
- 检索模式：
- 文档数量：
"""

[[files]]
relative_path = "docs/graph-guide.md"
content = """# AIASys 知识图谱使用指南

知识图谱通过 LLM 自动从文本中抽取实体和关系，构建可交互的关系网络。

## 创建图谱

使用 `CreateKnowledgeGraph` 创建新图谱。

## 添加文档并构图

使用 `UploadDocumentsToGraph` 上传资料。系统会自动：
1. 抽取实体（人物、组织、概念、事件等）
2. 抽取关系（属于、创造、影响、位于等）
3. 实体消歧（合并同名实体）
4. 更新图谱存储

## 查询方式

### 实体搜索
`SearchKnowledgeGraphEntities` — 按名称或描述搜索实体。

### 实体详情
`GetKnowledgeGraphEntityDetail` — 获取实体的完整属性、关系和来源文档。

### 关系查询
`QueryEntityRelations` — 查询两个实体之间的关系路径。

### 社区发现
`GetCommunityReport` — 生成社区报告，发现知识聚类和主题分布。

## 手工维护

- `CreateGraphEntity` / `UpdateGraphEntity` / `DeleteGraphEntity`：手工增删改实体
- `CreateGraphRelation`：手工创建关系

## 可视化

在前端 Knowledge > Graph 页面中可查看：
- 力导向图布局
- 社区着色
- 实体详情面板
- 关系过滤

## 当前工作区图谱配置

（Agent 在创建图谱后填写）

- 图谱名称：
- 图谱 ID：
- 实体数量：
- 关系数量：
- 社区数量：
"""
